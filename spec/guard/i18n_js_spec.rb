# frozen_string_literal: true

RSpec.describe Guard::I18nJs do
  it "has a version number" do
    expect(Guard::I18nJs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
